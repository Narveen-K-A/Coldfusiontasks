USE tech;

CREATE TABLE wufooForm (select_option varchar(30),
					   Joining date,
					   Web_site varchar(100),
					   firstName varchar(20),
					   lastname varchar(20),
					   Email varchar(40),
					   Phone varchar(20));

SELECT * FROM wufooForm;